"""Import the Account class from the Account.py file."""
# ADD YOUR CODE HERE
from Account import Account

# Define a function for the Savings Account
def create_savings_account(balance, interest_rate, months):
    """Creates a savings account, calculates interest earned, and updates the account balance.

    Args:
        balance (float): The initial savings account balance.
        interest_rate (float): The APR interest rate for the savings account.
        months (int): The length of months to determine the amount of interest.

    Returns:
        float: The updated savings account balance after adding the interest earned.
        And returns the interest earned.
    """
    # Create an instance of the `Account` class and pass in the balance and interest parameters.
    #  Hint: You need to add the interest as a value, i.e, 0.
    # ADD YOUR CODE HERE
    interest = Account(starting_balance)
    starting_balance = float(input)

    # Calculate interest earned
     # ADD YOUR CODE HERE
     def get_int_earned(self):
        return self.int_rate

    # Update the savings account balance by adding the interest earned
    # ADD YOUR CODE HERE
    get_balance(self + int.earned):
    return self.balance

    # Pass the updated_balance to the set balance method using the instance of the SavingsAccount class.
    # ADD YOUR CODE HERE
    class CD(SavingsAccount):
        def__init__(self, balance, interest):
        SavingsAccount.__init__(self, balance, interest)

    # Pass the interest_earned to the set interest method using the instance of the SavingsAccount class.
    # ADD YOUR CODE HERE
    savings_data = SavingsAccount(savings_interest)

    # Return the updated baance and interest earned.
    return  # ADD YOUR CODE HERE
    print("The balance is: $", format(savings_data.get_balance(), ',.2f'))
    print("APR Interest earned is: ", format(savings_data.get_savings_interest(), ',.2f')

